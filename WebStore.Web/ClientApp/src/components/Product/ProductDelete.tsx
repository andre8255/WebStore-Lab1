import React from "react"; import { Card, CardHeader, CardContent, CardActions, Button } from "@mui/material"; import { useNavigate, useParams } from "react-router-dom"; import axios from "axios"; export const ProductDelete = () => { const navigate = useNavigate(); const { id } = useParams(); const handleDelete = async () => { await axios.delete(`/api/ProductApi/${id}`); navigate("/product"); }; return (<Card><CardHeader title="Delete Product" /><CardContent>Are you sure?</CardContent><CardActions><Button onClick={handleDelete} variant="contained" color="error">Delete</Button><Button onClick={() => navigate("/product")} variant="contained">Cancel</Button></CardActions></Card>); };